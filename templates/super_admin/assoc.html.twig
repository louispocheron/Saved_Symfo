
{% extends 'base.html.twig' %}


{% block title %}
 superadmin lol  
{% endblock %}

{% block body %}
    <h1> liste des utilisateurs de {{ association.name }}</h1>

    {% for user in users %}
    <div>
        <a href=" {{path("super_admin_user_delete", {'id': association.id, 'userID': user.id} )}} "><i class="fa-solid fa-trash"></i></a>
        <a href=" {{path('super_admin_user', {"id": association.id, "userID": user.id }  )}} "><p>{{ user.username }}</p></a>
    <div>
    {% endfor %}


{% endblock %}
